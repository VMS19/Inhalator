[tox]
envlist = py3
skipsdist = True

[testenv]
passenv = *
deps =
    -r{toxinidir}/requirements.txt
    pytest
    pytest-cov
    freezegun
    pytest-xvfb

commands =
    pytest -s

[pytest]
testpaths = tests/
addopts = --cov-report=html --cov-report=term --cov=.
